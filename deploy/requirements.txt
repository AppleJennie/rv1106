# Bus DMS Server Python 依赖
#
# 生产运行只需要前两个（fastapi + uvicorn）；
# pytest / httpx 仅用于离线开发与测试（FastAPI TestClient 依赖 httpx），
# 生产环境可用 `pip install fastapi "uvicorn[standard]"` 精简安装。
# sqlite3 为 Python 标准库，无需安装。

# ---- 生产依赖 ----
fastapi>=0.110,<1.0
uvicorn[standard]>=0.29,<1.0

# ---- 测试依赖（生产不装） ----
pytest>=8.0,<9.0
httpx>=0.27,<1.0
